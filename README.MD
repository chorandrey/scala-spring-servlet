
### Example of Scala app using Spring WebMVC

**How to package application**

Run `sbt universal:packageBin`

Then resulting package will be available in `target/universal/scala-servlet-0.1.0-SNAPSHOT.zip`. Unzip this package and you can run it through `bin/scala-servlet` script